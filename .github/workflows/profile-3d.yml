name: GitHub-Profile-3D-Contrib

on:
  schedule: # Daily update
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v2
      - uses: yoshi389111/github-profile-3d-contrib@0.7.0
        env:
          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: ${{ secrets.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  }}
          Supermarcel10: ${{ github.repository_owner }}
      - name: Commit & Push
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A .
          git commit -m "generated"
          git push
          
